# 必須人工實測的清單

> ## ⚠️ v0.29：B、C、D 三組（部署相關）整組關閉
>
> 系統改為**學生自行在自己的電腦上安裝執行**（PLAN.md D57），所以
> 「部署之後才做得到」「需要 FreeBSD 環境」「只有學校答得出來」
> 三組共 17 項全部失去標的，連同三份部署文件一起下架（D60）。
> **原文保留在 tag `hosted-v1`**，若日後真的要架站就原樣重新開啟。
>
> ⚠️ **A 組（本機瀏覽器就做得完）一項都沒有少，而且它現在容易多了**：
> 以前只有老師一台機器；現在**每一個學生打開它就是一次真實的跨瀏覽器
> 測試**。A1／A2（Chrome 與 Firefox 各開一次六個展示）如果讓幾個學生
> 順手回報，這個專案最大的缺口就補起來了。
>
> ⛔ **A5a（每週開放當週內容）已移除**——沒有開放閘門了（D59）。


> **這份清單只收「自動測試守不住、只有你做得到」的項目。**
> 已經有測試盯著的東西一律不收——一份混著已完成項目的清單會被略過，
> 而被略過的清單等於沒有清單。
>
> 目前自動測試 **1386 項**（`pytest`，約 10–12 分鐘）。凡是這份清單沒有寫的東西，
> 預設就是「測試守得住」。
>
> 產生時間：v0.30（2026-09-03）。開學 **W1 = 9/9**，Fourier 級數 **W3 = 9/23**。

---

## 怎麼讀這份清單

每一項有四欄，而**「失敗長什麼樣」是最重要的那一欄**。

這個專案反覆撞到同一件事：**大部分的失敗是安靜的**。頁面不會壞、不會拋錯、
每一個數字看起來都對，只是內容是錯的——2S5 那個「形狀對、尺度錯三倍」的鋸齒波、
2S10 那個「一次都沒跑過卻完全正常」的冒煙測試、相圖那個「洩掉答案但不會報錯」的
插圖，共同點都是**沒有任何東西看起來不對**。所以每一項都寫明「你要看到什麼才算過」，
而不只是「去測一下」。

優先序用三級：

| 記號 | 意思 |
|---|---|
| 🔴 | **9/9 開學前** |
| 🟠 | **對學生正式開放前**（也就是部署完成的那一天，不得跳過） |
| 🟡 | 之後可以補 |

---

# A. 本機瀏覽器就做得完（你現在的電腦，不需要部署）

> ⛔ **開始之前先讀這一條陷阱，它會讓你得到一個假的「可以」。**
>
> **不要用 `http://localhost:8000` 或 `http://127.0.0.1:8000` 判斷「純 HTTP 行不行」。**
> `AudioWorklet` 是 secure-context-only，而瀏覽器**把 `http://localhost` 當成安全脈絡**
> ——所以在本機測，純 HTTP 永遠會給出「可以」這個**錯誤答案**，
> 而學生從別台機器連進來時展示區直接不能用（D43）。
> 本機測試只能回答「功能對不對」，回答不了「要不要 HTTPS」。那一題在 §B6。

## A1 🔴 六個展示在 Chrome 開一次（2S7 的一半）

**驗什麼**：展示區在真的瀏覽器裡跑得起來、出得了聲、圖畫得出來。
沙箱裡沒有瀏覽器，這一項從第一輪欠到現在。

**怎麼驗**：

```bash
export SESSION_SECRET=$(python -c "import secrets; print(secrets.token_hex(32))")
uvicorn app.main:app --reload
```

登入後逐一開這六頁，每一頁都要**按下 Start sound**、**拖動每一支滑桿**：

| 頁 | 網址 |
|---|---|
| 取樣與混疊 | `/demos/sampling/aliasing` |
| 頻譜洩漏 | `/demos/spectrum/leakage` |
| Fourier 加法合成 | `/demos/fourier/series` |
| 摺積與 LTI | `/demos/lti/convolution` |
| 脈衝與時頻取捨 | `/demos/transform/pulse` |
| 極零點與數位濾波器 | `/demos/filter/pole-zero` |

⚠️ **`/demos/selftest` 這個網址不存在**。PLAN §8.4「方案 C」把它列為構想，
但它**從來沒有被實作**（`grep` 整個 `app/` 找不到）。所以這一項沒有捷徑，
只能六頁逐一開。

**預期看到**：每一頁都有聲音；每一張圖在拖滑桿時**連續**變化；
瀏覽器 console（F12）**沒有紅色錯誤**。

**失敗長什麼樣**：

- ⚠️ **最危險的一種：圖在動、數字在變、但沒有聲音**，而你以為是自己的喇叭。
  ⇒ 先確認系統音量與分頁沒有靜音，再看 console 有沒有 `AudioWorklet` 的訊息。
- 圖是靜止的但沒有錯誤訊息 ⇒ `requestAnimationFrame` 那條路徑斷了
  （2S10 的冒煙測試就漏掉過一次同型的問題）。
- 頁面正常但 console 有 404 ⇒ 自架的資產（KaTeX、HTMX、worklet）路徑錯了。

## A2 🔴 同樣六頁在 Firefox 再開一次（2S7 的另一半）

**驗什麼**：Chrome 與 Firefox 就是官方支援的**全部**（D45），
所以這兩家跑過，展示區的瀏覽器相容性這一格就結案得掉——不必等部署。

**失敗長什麼樣**：Firefox 特別容易在 `AudioWorklet` 的載入路徑與
`AudioParam` 的斜坡上與 Chrome 分歧，而症狀同樣是**聲音不對而不是頁面壞掉**。

## A3 🔴⛔ 極零點那一頁：**先把音量調到最小、戴上耳機**

**驗什麼**：這是唯一一頁**有可能真的把喇叭弄壞**的展示。防護做了三層
（極點出圈就停止音訊／把 |H| 峰值壓回 1／worklet 裡的逐樣本看守），
但三層都沒有在真的瀏覽器裡被觸發過。

**怎麼驗**：

1. 系統音量調到最小，戴耳機（**先戴上再放到耳朵旁邊**，不要直接戴著開始）。
2. 把極點半徑滑桿推到最靠近單位圓的那一格，聽共振有沒有失控。
3. 把極點推出單位圓：**音訊應該停止，而且畫面上要出現一句英文說明**。
4. 慢慢把音量調上去，重複 2、3。

**預期看到**：第 3 步一定要有**畫面上的**訊息，不是只有 console。

**失敗長什麼樣**：極點出圈之後聲音愈來愈大（三層防護都沒生效）——
**這一項是唯一一個失敗會傷到人的**，所以順序不能顛倒。

## A4 🔴 相圖的箭頭在真的瀏覽器裡看一次

**驗什麼**：相圖的 `<marker>` 箭頭**在真的瀏覽器裡從來沒有被看過**。
沙箱是用 `cairosvg` 光柵化來看的，它把 marker 畫對了，
但它不是 Chrome 也不是 Firefox。

**怎麼驗**：出一題 `system.linear_2x2.*`（四個題型都出一次），
展開 Show Answer → Show Solution Steps，看最底下那張圖。

**預期看到**：方向場的灰色短箭頭**每一根都有箭頭**；藍色軌跡上有箭頭；
特徵方向線是綠色或紅色並帶 `λ = …` 標籤。

**失敗長什麼樣**：⚠️ **線都在、箭頭不見了**。那是這一項唯一會出的問題，
而它安靜——沒有箭頭的相圖裡，**穩定節點與不穩定節點長得一模一樣**（D50），
圖照樣渲染、照樣好看、只是說了相反的話。有一項測試守住「`marker-end`
指到的 id 存在」，那擋得住指錯 id，擋不住「這個瀏覽器不畫 marker」。

## A5 🔴 答案遮蔽在真的瀏覽器裡確認一次

**驗什麼**：規則 5——題目卡片三層，`Show Answer` 與 `Show Solution Steps`
兩層都預設收合，而**相圖只能在最內層**。

**怎麼驗**：隨便出三題（其中一題要是線性系統，那是唯一有插圖的），
**在還沒有點任何東西之前**看整張卡片。

**預期看到**：只看得到題目與方程式。答案、步驟、相圖**一個都看不到**。

**失敗長什麼樣**：有測試盯著預設收合，所以純粹漏掉 `open` 的情況會變紅。
這一項要人看的是測試看不到的那半：**捲動位置、字級、行寬會不會讓答案
剛好露出一角**，以及**列印或投影時 `<details>` 會不會被展開**
（投影是實際用法之一）。

## ~~A5a 開學前把第 1 週的內容開起來~~ — ⛔ 已移除（v0.29，D59）

沒有開放閘門了。學生打開系統就看得到全部內容。

## A5b 🔴⛔ 推上 GitHub 之前的三項確認（`PUBLISHING.md` §0）

**驗什麼**：`git push` 推的是**整段歷史**，而這個專案在 v0.29 之前有帳號與
SQLite 資料庫。**推上去之後就收不回來了。**

**怎麼驗**：`PUBLISHING.md` §0 有三個指令（版控裡的髒東西、歷史裡曾經加入
過的、明碼密碼）。三項在 v0.30 都實際跑過一次，結果與逐項的解讀寫在那裡。

**預期看到**：與那張表相同。⛔ **不同的話先弄清楚多出來的是什麼再推。**

**失敗長什麼樣**：一個 `.db` 檔或一份真的憑證被推上公開的 repo——
而**它不會有任何症狀**，直到有人去看。

## A5c 🔴 從 GitHub clone 一份，照著安裝說明從零裝一次（＝舊的 B7）

**驗什麼**：兩份安裝說明是這一輪最大的缺口——**它們沒有在一台乾淨的機器上
被照著走過**。

**怎麼驗**：`PUBLISHING.md` §4。從 `/tmp` clone 一份新的，建 venv，
`pip install`，起 uvicorn。

**預期看到**：四個指令都不需要看疑難排解那一節。

**失敗長什麼樣**：⚠️ **在你自己的機器上永遠不會失敗**——你早就有 Python、
有相依、有整個資料夾。所以這一項一定要在**新 clone 的目錄**裡做，
最好是在一台沒裝過的機器上（或請一個學生做，那更準）。

## A6 🔴 人工審題一輪（PLAN 的工作項 **2c**，唯一還沒做過的那一輪）

**驗什麼**：漂亮度評分是自動的**下限**，人工審題是品質的**上限**（§2.8）。
測試守得住「殘差是 0」，守不住「這題出得好不好、敘述像不像上課的講法」。

**怎麼驗**：

```bash
python scripts/preview.py -n 5      && open preview.html      # 240 題
python scripts/review_steps.py -n 5 && open 2c-PRESCREEN.md   # 機器初篩（v0.32）
```

樣本已備妥（v0.35 重新產過）：`preview.html` 涵蓋全部 **11 個題型 × 3 個難度 × 5 題
= 165 題**。⚠️ **v0.35 之前是 16 個題型、240 題**——老師刪掉了五個（PLAN D67），
所以 v0.32 那一輪對被刪題型做的初篩結果已經沒有標的了。

> **⚠️ v0.32 新增一份機器初篩：`2c-PRESCREEN.md`。**
> 它把 240 題先過一遍，依五項**機器讀得出來**的判準（步驟顆粒度、
> `note` 在說「為什麼」還是「做了什麼」、難度階梯的三個可量向度、
> 英文用語的分佈、附錄 C 的 $y$ vs $y(x)$）排出「先看這幾個」。
>
> ⛔ **它不是判斷，是排序。** 每一項判準都會誤報，理由逐項寫在那份檔案裡與
> `scripts/review_steps.py` 的檔頭。**用它決定從哪一格開始看，不要用它決定
> 哪一格不用看。**
>
> ⚠️ **AI 刻意沒有改任何一句敘述**——2c 的判斷是老師的，而一個
> 「順手改得比較順」的敘述會讓這一輪的產出從「老師的品味」變成「AI 的品味」，
> 而且沒有人看得出來換過。

逐題看，特別注意三件測試抓不到的事：

1. **難度階梯合不合理**——難度 2 是不是真的比難度 1 難，
   還是只是「式子比較長」。
2. **英文用語對不對得上課本**（§7 #11：`general solution` vs
   `complete solution`、`integrating factor` 怎麼引入）。
3. **附錄 C 的符號**——特別是 `separable.py` 已知的那一處：
   中間步驟寫 $y$、最後一步才寫 $y(x)$（§7 #29）。

**失敗長什麼樣**：不會有錯誤訊息。你會在第 30 題突然覺得「這題怪怪的」，
而那個感覺就是這一輪的產出。看到就記下來。

> **初篩指出來的幾件事**（⚠️ **下面的數字是 v0.35 重新產過的**，
> 只涵蓋還在的 11 個題型；完整表格見 `2c-PRESCREEN.md`）：
>
> - **`initial condition` 與 `initial value` 兩種說法都在用**——§7 #11 要拍板的
>   正是這一類。其餘三組競爭說法都只用了一種。
> - **有幾個題型「步驟寫裸露的 $y$、答案寫 $y(x)$」**——§7 #29 原本只記了
>   `separable.py` 一處，**實際上不只一處**。
> - ⚠️ **「一條線索都沒有」不等於沒問題**——那五項判準看不出「敘述像不像上課的講法」。

## A7 🟠 相圖的人工審查（§2.11.4 第四層）

```bash
python scripts/preview.py --portraits && open preview-portraits.html
```

**預期看到**：每一張圖都**讀得出來**——看得出是鞍點還是節點、
軌跡沒有全擠在角落、標籤沒有疊在座標軸上。

**失敗長什麼樣**：⚠️ 前三層測試**每一條斷言都會過**，然後圖因為兩條特徵方向
接近平行而擠成一團墨。已知的弱點：**特徵方向接近平行的鞍點圖是擠的**，
而我們**刻意沒有為了圖去限制出題**。你要判斷的是「擠到不能用了嗎」。

## A8 🟡 無障礙一輪（工作項 **2S8**）

**驗什麼**：D30 限定桌機時明文寫著無障礙**不放寬**——它服務的是桌機上的
視障與聽障學生。

**怎麼驗**（四件事，每一件五分鐘）：

1. **鍵盤走得完**：把滑鼠收起來，只用 Tab / Shift-Tab / 方向鍵 / Enter
   把一個展示頁從頭操作到尾。
2. **螢幕閱讀器讀得到數值**：macOS 開 VoiceOver（⌘F5），聽滑桿的值念不念得出來。
3. **`prefers-reduced-motion`**：系統設定打開「減少動態效果」，重新整理，
   捲動頻譜圖之類的動畫應該停下來或變慢。
4. **不只靠顏色**：⚠️ **已知有一格沒解決**——退化節點只有一條特徵方向線，
   顏色是它唯一的視覺線索，色盲讀者只剩數值標籤（D50 明文記著這一項留給 2S8）。
   你要決定的是「數值標籤夠不夠」。

**失敗長什麼樣**：全部都是「用得了但很難用」，不會有任何錯誤。

## A9 🟡 HiDPI／投影

**怎麼驗**：把筆電接上外接螢幕或投影機，開一個展示頁。

**預期看到**：圖是銳利的，字看得清楚。

**失敗長什麼樣**：一張糊圖。DPR 縮放做了但沒有在真的外接螢幕上看過，
而**投影出來會比在筆電上明顯得多**（D30 第 2 點）。

## A10 🟡 Safari（**只有你有 Mac 才做；沒有的話這一格就是空的**）

**驗什麼**：D45 宣告不支援 Safari，而那個宣告有實作（兩層偵測）。
但 `browser.js` 的引擎判準**在真的 Safari 上一次都沒有跑過**——
測試用的 UA 是抄下來的字串。

**怎麼驗**：Safari 開任一展示頁。

**預期看到**：頁面上出現一段 warning，內容點名 Chrome 與 Firefox。

**失敗長什麼樣**：⚠️ **沒有任何訊息**。那表示 Safari 被誤判成「支援」，
於是學生靜默地聽到錯的聲音——**與 D45 想擋的失敗一模一樣**。

> **這一項如果做不了，就讓它空著，不要假裝它過了。**
> 日後若有學生反映「Mac 上沒有聲音而且沒看到任何訊息」，**先看這一格**。

---

# ~~B. 部署之後才做得到~~ — ⛔ **整組關閉（v0.29，D60）**

> 沒有部署了（D57／D60）。B1（反向代理的存取紀錄）、B2／B3（校內允許清單與 VPN）、B4、B5（憑證）、B6（純 HTTP 的失敗訊息）全部失去標的。
> 
> ⚠️ **B7（學生的完整流程走一次）沒有失去標的，它換了形狀**：現在它就是「照著 `INSTALL-LINUX.md` 或 `INSTALL-MACOS.md` 從零裝一次」，而**那件事只有在一台沒有裝過的機器上做才算數**——你自己的機器上早就有 Python 與相依了。
> 
> ⚠️ **B8（多人並發）真的消失了**，而且是好消息：每個人跑自己的一份。
>
> 原文保留在下面供對照。

> 這一整組的共同點是：它們**全部在我們的行程外面**，
> 所以應用層那 1355 項測試沒有一項守得住任何一項。

## B1 🟠⛔ 反向代理的存取紀錄裡沒有 IP（PLAN §7 #40）

**驗什麼**：D38「存取紀錄不得含用戶端 IP」有三層。應用層與 uvicorn
那兩層各有測試盯著，**第三層（Caddy／nginx）沒有測試，也不可能有**。

**怎麼驗**：部署完成後隨便打幾個請求，然後

```
tail -50 /var/log/caddy/engmath.log
```

用眼睛掃一遍。

**預期看到**：裡面**一個 IP 位址都沒有**。

**失敗長什麼樣**：⚠️ **這是整份清單裡最容易被忘記的一項，因為它不影響任何功能。**
設定載入成功、網站完全正常、每一項測試全綠，**而 log 檔裡每一行都有一個 IP**。
`format filter` 與 `delete` 的語法在 Caddy 版本之間會變，寫錯不會有任何錯誤訊息。

> ⚠️ **這一項在做完之前，D38 不算結案。**
> 而且**家裡預演過也不算**——學校那台的 Caddyfile 會多一段允許清單，
> 那正是最可能讓人「先把 log 打開看看擋掉了誰」然後忘了關的時候。

## B2 🟠 校內允許清單真的擋得住（`FREEBSD-DEPLOY.md` §8 #13）

**怎麼驗**：**從校外的網路**（手機開熱點就可以）開網站首頁。

**預期看到**：一頁英文說明（403），內容是「請先連學校 VPN」。

**失敗長什麼樣**：⚠️ **設定載入成功、校外照樣進得去。**
`import` / `error` / `reverse_proxy` 在同一個站台區塊裡的預設排序沒有人確認過。
**不要靠讀設定檔判斷這件事**——只有從校外實測算數。

## B3 🟠⛔ VPN 的來源位址：三步，缺一不可（PLAN §7 #41）

**驗什麼**：學校 VPN 連進來的位址**會不會落在你填的校內網段裡**，
沒有人知道，而且**不能用猜的**。

**怎麼驗**（需要一台在校外的機器，三步都要做）：

1. 校外網路直接存取 → **應該看到被擋的那一頁**
2. 同一台機器連上學校 VPN，再存取 → **應該進得去**
3. 斷開 VPN，再存取 → **應該又被擋**

**失敗長什麼樣**：第 2 步進不去。⚠️ **這是整份清單裡唯一一個「失敗會讓一頁
對學生說謊」的**：一個校外的學生照著被擋頁面的指示連上了 VPN，**還是**被擋，
於是那一頁的每一句話都變成假話，而他**沒有第二個辦法可以試**。

> ⚠️ **不能用「問網路中心 VPN 的網段是多少」取代這三步。**
> 那個答案是**設定值**，而我們要驗的是**封包到達這台機器時長什麼樣**——
> 中間任何一次 NAT 都會讓兩者不同。
>
> **這一項在做完之前，D42 不算結案。**

## B4 🟠 允許清單沒有誤傷應用程式自己的 403（`FREEBSD-DEPLOY.md` §8 #14）

**怎麼驗**：**在校內**用 `class` 帳號（不是 `staff`）開 `/activity`。

**預期看到**：應用程式自己的「只給 staff」那一頁。

**失敗長什麼樣**：看到的是「Campus network required」。那表示 Caddy 的
`handle_errors` 把後端回的 403 也接管了——推論說不會，但**「推論不會」不是「驗過不會」**。

## B5 🟠 憑證，**而且第六天要再看一次**（`FREEBSD-DEPLOY.md` §8 #8）

**怎麼驗**：部署當天用瀏覽器確認鎖頭正常。

**⚠️ 然後在行事曆上排一個「第 7 天」的提醒，再開一次網站。**

**失敗長什麼樣**：⚠️ **這一項的失敗模式是「第六天才壞」。**
Let's Encrypt 的 **IP 位址憑證只發 `shortlived` profile（效期約六天）**，
而 Caddy 對「IP + shortlived」的支援程度查不清楚（該專案有一個相關的未結 issue）。
部署當天一切正常證明不了任何事。

> **最省事的解法不是驗它，是繞過它**：爭取一個網域名稱，這個問題整個消失（§D4）。

## B6 🟠 純 HTTP 的失敗訊息（D43／`FREEBSD-DEPLOY.md` §8 #17）

**驗什麼**：`AudioWorklet` 在非安全脈絡下**到底怎麼失敗**沒有查證過
（拋例外／回 undefined／靜默不出聲，是哪一種不知道）。

**怎麼驗**：**從別台機器**（不是伺服器本機）用 `http://<位址>/demos/...` 開一次。

**預期看到**：畫面上有一段英文訊息，內容提到需要 HTTPS。

**失敗長什麼樣**：沒有聲音、**也沒有任何訊息**。那不擋上線（我們本來就走 HTTPS），
但它會告訴你「如果哪天有人把 HTTPS 弄掉了，學生會不會知道」。

> ⛔ 再說一次：**不要用 `http://localhost` 做這一項**，那會給你假的「可以」。

## B7 🟠 學生的完整流程走一次

**怎麼驗**：用**學生的那組共用帳號**（不是 `staff`），從登入到出題到展開答案，
在**校內的一台不是你自己的電腦**上走一次。

**預期看到**：登入後 cookie 正常（`COOKIE_SECURE=1` 在 HTTPS 後面要能過）、
出題頁能出題、`/activity` **進不去**（那是 staff 專屬）、
**`/admin/content` 也進不去**（v0.28）。
⚠️ 順便試一個**還沒開放**那一週的展示網址（例如 `/demos/filter/pole-zero`），
應該是 404 ——那是 D55 在真實部署上唯一的人工確認點。

**失敗長什麼樣**：登入後被踢回登入頁——那通常是 `COOKIE_SECURE` 與代理設定
對不上，而錯誤訊息不會說這件事。

## B8 🟡 多人並發（開學後第一堂課）

**怎麼驗**：課堂上請全班同時打開一個展示頁。

**失敗長什麼樣**：出題變慢（SymPy 是 CPU 密集的）。
在自己電腦上開十個分頁**不是**三十個學生。

---

# ~~C. 需要 FreeBSD 環境（家用預演或學校那台）~~ — ⛔ **整組關閉（v0.29，D60）**

> 沒有 FreeBSD 了（D60）。⚠️ **C1 換了形狀而不是消失**：「`pytest` 在別的 Python 版本上全綠」仍然值得做一次，因為學生的機器上什麼版本都有（`INSTALL-*.md` 寫的是 3.10 以上，而那個下限沒有在 3.10 以外的版本上實測過）。
>
> 原文保留在下面供對照。

> ⚠️ **`FREEBSD-DEPLOY.md` 與 `FREEBSD-HOMELAB.md` 全文都是在 Linux 沙箱裡寫的，
> 沒有一件事在 FreeBSD 上跑過**，所以整組都在這裡。
> 好消息是這一整組**在家裡就驗得完**（`FREEBSD-HOMELAB.md` §10.1），
> 不必等學校那台。

## C1 🟠 `pytest` 在 FreeBSD + Python 3.11 上全綠

**怎麼驗**：照 `FREEBSD-DEPLOY.md` §7 走到第 6 步。

**預期看到**：**1355 項全過**（沒有裝 node 的話會有一批 skip，
而且它會**印出跳過的原因**——那是刻意的，skip 不是通過）。

**失敗長什麼樣**：專案宣告的下限是 Python 3.10，FreeBSD 預設是 3.11，
而**這個專案沒有在 3.11 上跑過**。「應該沒問題」不是「跑過」。

## C2 🟠 pkg 裝出來的版本滿足 `requirements.txt`

**怎麼驗**：`pkg info | grep py311-`

**預期看到**：⛔ **`sympy` 必須是 `1.14.x`**、`pydantic` 必須是 2.x。

**失敗長什麼樣**：⚠️ SymPy 版本不同的症狀**不是壞掉**——`dsolve` 與
`latex()` 的輸出**形式**會變（`(C1 + C2*x)*exp(-2*x)` vs
`C1*exp(-2*x) + C2*x*exp(-2*x)`），於是題目照樣出得出來、答案照樣正確，
只是**跟你審過的樣本長得不一樣**。C1 那一步會抓到大部分，但不是全部。

## C3 🟠 rc.d 腳本的三件事（`FREEBSD-DEPLOY.md` §8 #6、#16）

**怎麼驗**：

```sh
service engmath start
ps -o user,command -p $(cat /var/run/engmath.pid)   # user 要是 engmath、command 要是 daemon
service engmath status                              # 要說得出 pid
service engmath stop                                # 要真的停
fetch http://127.0.0.1:8000/healthz                 # 停之前要回 {"status":"ok"}
```

**失敗長什麼樣**：⚠️ **v0.19 修掉的兩個 bug 的症狀都不是「壞掉」**——
一個是服務**以 root 執行**（跑得好好的），一個是 `service status` / `stop`
**對不上 pid**（服務照樣在跑）。所以「沒人抱怨」證明不了新版是對的。

## C4 🟠 開機自動啟動

**怎麼驗**：`reboot`，開機後直接 `fetch http://127.0.0.1:8000/healthz`。

## C5 🟡 newsyslog 輪替之後 log 還在寫（`FREEBSD-DEPLOY.md` §8 #7）

**怎麼驗**：`newsyslog -F` 手動輪替一次，然後打幾個請求，看新的 `app.log` 有沒有長大。

**失敗長什麼樣**：⚠️ **log 停止成長，而服務完全正常。**
`daemon -o` 開的檔案被 rename 之後不會自動重開；我們靠 `daemon -H`（SIGHUP 重開輸出檔），
而 newsyslog 的訊號欄要**留空**。這件事沒有人跑過。

## C6 🟡 檔案權限與 `nologin` 服務帳號

**預期看到**：資料庫 0600、session 金鑰檔 0400、服務帳號 shell 是 `nologin`。

## C7 🟠 備份腳本跑一次，**而且還原一次**

**怎麼驗**：跑一次 `backup.sh`，然後**把備份還原到另一個路徑並開起來**。

**失敗長什麼樣**：⚠️ 只驗「備份檔產生了」是不夠的——SQLite 開著 WAL，
**檔案系統的快照抓到的是 WAL 的當下狀態，那不等於應用層的一致點**。
一份還原不回來的備份，在需要它的那一天才會發現。

## C8 🟠 `pf` **不要**擋 80／443

**怎麼驗**：從外面 `nc -zv <位址> 80` 與 `443`，兩個都要通。

**失敗長什麼樣**：⚠️ **它不會在部署當天壞，會在第六天壞**——
擋掉 80 之後 Let's Encrypt 驗證不到，而憑證是六天一期。
而且**沒有「把 LE 加進允許清單」這個選項**：LE 不公布驗證來源位址、且強制多視角驗證。

---

# ~~D. 只有學校（計中／網路中心）答得出來~~ — ⛔ **整組關閉（v0.29，D60）**

> 沒有要向學校要任何東西了（D60）。
>
> 原文保留在下面供對照。

> 這一組**前置期最長**，所以即使它們排在部署那一天才用得到，
> **信要現在就寄**。

## D1 🔴 校內網段的正確清單——**四項，缺一不可**（PLAN §7 #42）

要問到的是：

1. 有線與無線（**含 eduroam**）的**全部**對外網段
2. ⛔ **IPv6 網段**——只填 IPv4 而主機有 AAAA 記錄的話，
   **走 IPv6 的校內學生會被自己的允許清單擋掉**
3. **宿舍網路算不算校內**（若宿舍走另一個 ISP，住宿生在自己房間裡就是「校外」，
   這會決定 VPN 說明要寫得多顯眼）
4. 這些網段**多久變一次、變的時候會不會通知**

**失敗長什麼樣**：⚠️ `FREEBSD-DEPLOY.md` §5.8 的設定範例裡**一個真實網段都沒有填**，
全部是 RFC 5737 的文件用位址當佔位符。這是刻意的：**寫一個看起來很像的網段進去，
比留一個明顯的空格危險得多**——猜錯的方向是**把全班擋在門外**，
而那個症狀在你自己的機器上（在校內）**永遠不會出現**。

## D2 🟠 那個固定 IP 是不是公開可路由的

**怎麼驗**：`ifconfig`，並向計中確認。

## D3 🟠 防火牆開通與資安檢查的流程

**驗什麼**：NTNU 要不要報備、要不要過資安檢查、流程多久。
這是人的流程，**只能問**。

## D4 🟠 能不能要到一個網域名稱

**為什麼值得問**：一個網域名稱讓 §B5（六天一期的 IP 憑證）**整個問題消失**。
`FREEBSD-DEPLOY.md` 的建議一直是「優先爭取網域名稱」，而它的成本是一封信。

---

# E. 不是實測，但只有你決定得了（而且它們擋著東西）

> 這一組放進來的理由：它們散在 PLAN §7，而**每一項都有一個會過期的時點**。
> 不是「有空再說」的那種待辦。

## E1 🔴⛔ Fourier 的 $a_0$ 要不要除 2（PLAN §7 #23）

**要決定什麼**：級數寫成 $\frac{a_0}{2} + \sum(\cdots)$ 還是 $a_0 + \sum(\cdots)$。
兩種課本都有。目前**照暫定值（除 2）實作了**。

**改的成本**：**程式上幾乎是零**——整個專案只有 `fourier/core.py` 的
`A0_IS_HALVED` 一個布林值決定它，有一項測試翻轉那個布林值並確認五個導出量
全部跟著變。

**⚠️ 但真正的成本在時機，而且它與程式無關**：課綱 W3 是 **9/23**，
學生會在期中考前練。**題目一旦印給學生看過，他們的筆記就對不上了**——
而那個不一致不會讓任何東西壞掉，只會讓一批學生把兩種慣例混在一起背。

**⇒ 這一項要在 9/23 之前拍板。**

## E2 🟡 參數變異法的 $g(x)$ 白名單（PLAN §7 #14）

**要決定什麼**：從課本與考古題圈出四到六組 $g(x)$。隨機生成幾乎不可能得到
積得出來的形式，所以這一項**擋著整個 2e**。

**建議的起點**（圈選或增刪即可）：$y''+y$ 配 $\sec x$／$\tan x$／$\csc x$，
$y''-y$ 配 $\frac{1}{1+e^x}$，$y''+3y'+2y$ 配 $\frac{1}{1+e^x}$，
$y''-2y'+y$ 配 $\frac{e^x}{x}$ 或 $\frac{e^x}{x^2}$。

**四到六組就夠**——這個題型的教學重點是 Wronskian 的流程，不是 $g$ 的多樣性。

## E3 🟡 Fourier 的課本範圍（PLAN §7 #24、#28）

三件事：（a）只到級數還是也含變換／積分；（b）含不含複數形
$\sum c_n e^{in\pi x/L}$；（c）含不含逐項微分／積分與收斂定理。

**⚠️ (a) 若含變換，還要決定 $2\pi$ 放哪裡**——三種慣例都有課本在用，
而 SymPy 的 `fourier_transform` 只實作其中一種。**做錯慣例等於整個題型重寫**，
所以這一項不確定之前，Fourier 變換的題型不排入。

## E4 🟡 展示區的三個介面決定（PLAN §7 #33–#35）

（a）展示要不要登入（目前要；代價是你在課堂上投影前得先登入，
學生也沒辦法把連結傳給沒帳號的人）；（b）導覽列要不要加 `Demos` 入口；
（c）哪些展示是課堂投影用、哪些是留給學生自己玩（會改變字級與說明文字的設計）。

---

# 最小集合：如果你只有一個小時

按這個順序做，**60 分鐘**：

| # | 項目 | 時間 | 為什麼是它 |
|---|---|---|---|
| 1 | **A3**（極零點，先調小音量戴耳機） | 5 分 | **唯一一個失敗會傷到人的**，所以它排第一，而且要在 A1 之前 |
| 2 | **A1 + A2**（六個展示 × Chrome、Firefox） | 25 分 | 展示區從第一輪欠到現在都沒有在瀏覽器裡跑過，而**兩家跑完這一格就結案**（D45） |
| 3 | **A4**（相圖箭頭） | 5 分 | 失敗是安靜的，而且**沒有箭頭的相圖會說相反的話** |
| 4 | **D1**（寄信給計中要網段清單，含 IPv6 與宿舍） | 10 分 | **前置期最長的一項**，而且它擋著部署那一天的每一件事 |
| 5 | **E1**（$a_0$ 除不除 2，拍板） | 10 分 | 不用做事，只要決定。**改的成本會在 9/23 之後從零跳到「一批學生的筆記」** |
| 6 | 剩下 5 分鐘 | | 把上面五項的結果寫回這份檔案（見下） |

**這一小時刻意沒有放的東西**，以及為什麼：

- **B 組全部**：還沒部署，做不了。
- **C 組全部**：需要 FreeBSD，而它整組要半天以上。
- **A6 人工審題**：它值得做，但**它需要的是安靜的兩小時，不是一小時裡的十分鐘**
  ——草草掃過去等於沒有審，而且會讓你以為審過了。

---

# 做完之後：把結果寫回來

> ⚠️ **不要只在心裡記得。**
>
> 這份清單與 `FREEBSD-DEPLOY.md`、`FREEBSD-HOMELAB.md` 的讀者是**半年後的你**。
> 那時你會記得「好像測過」，但不會記得「那個 uvicorn 的路徑到底要寫哪一個」。
>
> **PLAN v0.18 抓到兩處停在 v0.15 的失效敘述，v0.20 抓到整份 HOMELAB 的前提
> 在被執行之前就已經過時——三次的原因一模一樣：沒有人照著它做，
> 就沒有人撞到它是錯的。**

做法：在每一項標題後面補一行

```
> 實測 2026-09-05：✅ 過 / ❌ 實際是這樣：…
```

**「試過，不對，實際是這樣」比「✅」有價值得多**——前者會救到下一次，
後者只是讓清單變短。

**而沒做的項目請讓它空著。** 一份標成全綠的清單，與一份誠實留白的清單，
差別不在好看，在於半年後你還相不相信它。
