# 人機協作筆記

這份文件記錄**這個專案是怎麼被做出來的**——不是它做了什麼（那在
`README.md`），也不是為什麼那樣設計（那在 `PLAN.md`），而是「一個人加上一個
AI，用什麼方式合作了一個月」。

寫給三種讀者：

1. **接手繼續開發的人**（老師自己、或用自己的 AI 接手的學生）。
2. **想學人機協作的人**——這是一份真實紀錄，不是教學範例，所以它包含
   做錯的部分。
3. **半年後的我們自己**，那時沒有人記得為什麼要有 `.attic/`。

---

## ⚠️ 先說清楚這份文件缺了什麼

**前面約二十輪派送（dispatch）的原始提示詞沒有保存下來。**

這個專案的工作方式是：老師在 Claude Cowork 裡送出一段任務描述（一次
「dispatch」），AI 在一個**獨立的工作階段**裡完成它。獨立是關鍵——
每一輪的 AI 看不到前面幾輪的對話，它能依靠的只有**留在檔案裡的東西**。

那個性質有一個代價，而它到 v0.29 才被發現：**對話本身不是專案的一部分。**
每一輪結束，那段提示詞就留在應用程式的紀錄裡，而不在 repo 裡。

所以這份文件裡的「歷史」分成三種，而它們的可信度**不一樣**，
文件裡會逐處標示：

| 種類 | 來源 | 可信度 |
|---|---|---|
| **逐字紀錄** | v0.28、v0.29 兩輪的提示詞（見 `dispatches/`） | 原文 |
| **重建的歷史** | `PLAN.md` 的 D 決定表與變更歷程、102 個 commit 訊息、`TURNAROUND.csv` | 高，但**它記的是決定與結果，不是當初的要求** |
| **推測** | 無 | 這份文件不做推測。看不出來的地方就寫「看不出來」 |

**這個缺口的補救**是 `dispatches/` 目錄與寫在它 README 裡的慣例：
從 v0.29 起，每一輪開工的第一件事就是把提示詞原文存進去。

> **這件事本身就是一個值得記下來的教訓，而且它很有代表性：**
> 這個專案花了大量力氣讓「決定」不會遺失（`PLAN.md` 六千行、
> 九條硬規則、每個檔案開頭幾十行的理由），卻**沒有人想過要保存「要求」**。
> 結果是：我們知道每一個決定為什麼那樣做，卻不完全知道當初被要求做什麼。
> ⚠️ 一個系統可以在它最在意的那個面向上做到很好，同時在一個沒有人提過的
> 面向上完全空白——而空白不會發出任何聲音。

---

## 一、這個專案的協作模型

```
老師（一次一段文字，通常 300–800 字）
        │
        ▼
一個獨立的 AI 工作階段（看得到整個 repo，看不到前面的對話）
        │  讀 CLAUDE.md → 讀 PLAN.md 相關章節 → 動手 → 測試 → 提交
        ▼
一份回報（決定、數字、踩到的坑、還不夠穩的地方）
```

**AI 沒有記憶，repo 有。** 這一句話決定了這個專案幾乎所有的慣例：
凡是下一輪需要知道的東西，都必須寫進檔案裡，而且要寫在**下一輪一定會讀到
的地方**。

### 三個層次的「記憶」

| 檔案 | 存什麼 | 為什麼在那裡 |
|---|---|---|
| `CLAUDE.md` | **不可違反的規則**＋**已經不存在的東西** | 每一輪的第一份讀物。它有一大半篇幅在講「你在舊註解裡看到的 X 已經被拆掉了」——因為 AI 會很自然地照著一個已經作廢的設計往下寫 |
| `PLAN.md` | 每一個決定的**理由與代價**（D1–D61） | 決定本身很短，理由很長。半年後有價值的是理由 |
| 原始碼裡的檔頭與註解 | 「這一行為什麼長這樣」 | 距離最近。一個會被誤改的地方，理由要寫在那一行旁邊，不是寫在文件裡 |

---

## 二、真正有效的幾個慣例

這一節是這份筆記最可能對別人有用的部分。每一條都是**踩到問題之後才加的**，
不是一開始就想好的。

### 1. 決定要記「代價」，不只記「結論」

`PLAN.md` 的決定表每一列的格式是：**做了什麼決定 → 為什麼 →
被推翻的是什麼 → 代價是什麼**。

有代價那一欄，才看得出一個決定什麼時候該被重新考慮。例如 D35（共用帳號）
寫著「付出的是老師看不到個別學生的使用情形」——所以當 v0.29 老師說
「我另外想辦法評估」時，一眼就知道那一欄已經被接受了。

⚠️ **被推翻的決定不刪除，標記為已捨棄。** 這件事做過四次（D12 判分、
D32/D35 註冊與帳號、D41 對話介面、D57–D61 站台化），理由每次都一樣：
**一張看不出「這裡本來有東西」的文件，會讓後人以為從來沒有考慮過。**

### 2. 「這件事壞掉的時候會有人發現嗎？」

這是整個專案最常出現的一個問句，而它決定了測試要寫哪些。

反覆出現的模式是**安靜的失敗**——頁面不會壞、不會拋錯、每個數字看起來
都對，只是內容是錯的：

- 忘記把答案收合 → 答案直接印在畫面上，而**改程式的人已經知道答案，
  不會覺得哪裡不對**。
- 相圖跑到題目敘述旁邊 → 一張鞍點圖等於告訴學生兩個特徵值異號。
- 鋸齒波的尺度錯三倍 → 形狀完全正確，圖看起來很合理。
- 冒煙測試從來沒有真的跑過 → 它一直是綠的。

**這一類東西每一個都有一項專門的測試，而測試的 docstring 寫的是
「它壞掉的樣子」而不是「它應該做什麼」。** 那個寫法有一個實際的好處：
下一個想刪掉這項測試的人會先讀到刪掉的後果。

### 3. 讓「忘記做」變成紅燈

凡是「新增 X 的時候要記得也做 Y」的地方，都設法讓忘記變成測試失敗：

- 新增題型忘了歸到某一週 → `test_every_registered_template_has_a_week` 紅。
- 動了測試卻沒更新工時紀錄 → `test_the_last_row_knows_how_many_tests_there_are` 紅。
- 資料庫多一張表 → 一行寫死的表名斷言紅，**逼人重新想一次
  「它會不會存到指得到人的東西」**。
- 新增一條路由卻沒分類（v0.28 的閘門） → 列舉整張路由表的測試紅。

⚠️ **守不住的就明說守不住。** 例如「回報必須是真的」這條規則，
`CLAUDE.md` 裡寫著「這一條沒有測試守得住，而這裡不假裝有」。

### 4. 選 middleware 而不是相依注入——因為預設值的方向

這個判準在專案裡出現了三次（D33 個資告知、D37 登入、D55 內容開放），
論證每次都一樣：

> 相依注入的正確性依賴「每一個路由都記得掛上」，而**漏掉一個不會拋錯、
> 不會讓任何測試變紅——它只會安靜地開一個洞**。middleware 的預設值反過來：
> 沒有被明確豁免的路徑一律擋下。

**寫下判準本身，比寫下結論有用。** v0.28 那一輪 AI 是照著這段論證做的，
而且**同時說明了它判不了的那一半**（表單 body 裡的識別碼），沒有假裝
middleware 什麼都能做。

### 5. 記錄牆鐘時間（`TURNAROUND.csv`）

老師想知道的是：`PLAN.md` 估的人週，實際請 AI 執行要花多少**牆鐘時間**。
人週是估計，牆鐘時間只能一列一列量。

八輪下來的實測比值是 **1 人週 ≈ 1.65 牆鐘小時**，樣本 8 列。
⚠️ 那是一個外插，不同性質的工作（出題引擎 vs 前端展示 vs 文件）
很可能有完全不同的比值。

| 工作項 | 估計（人週） | 實際（牆鐘小時） |
|---|---|---|
| 2S10 摺積展示 | 0.5 | 1.13 |
| 2S11 脈衝展示 | 0.3 | 4.96 ⚠️ 含 4.00 小時配額中斷 |
| 2S9 極零點展示 | 0.6 | 1.07 |
| 2f Laplace | 0.6 | 1.01 |
| 2B0–2B4 Fourier | 2.2 | 1.59 |
| 2d + 2B6 系統與相圖 | 1.9 | 1.39 |
| 2a + 2b 待定係數與恰當 | 0.9 | 0.92 |
| 2R 週次歸類與閘門 | 0.8 | 0.82 |

**最值得注意的是 2B0–2B4 那一列**：規劃估 2.2 人週（單輪最大），實際 1.59
小時——比 0.5 人週的 2S10 還快。看得出來的解釋是：**Fourier 那一輪的規格
在動手之前就寫得非常完整**（PLAN §2.10 有四層閘門的完整設計與 SymPy 實測），
而展示那幾輪的規格是「做一個能聽出混疊的頁面」。
⚠️ 這是一個從八個樣本讀出來的印象，不是一個結論。

### 6. 環境的限制會反過來塑造慣例

這個 AI 工作階段把專案資料夾以 FUSE 掛載進沙箱，權限是
**可建檔、可覆寫、可改名，但不可刪檔**。這個限制生出了三個慣例：

- **`scripts/git-safe-commit.sh`**：git 幾乎每個寫入都是「建 `.lock` →
  寫 → rename → 刪掉殘餘」，最後那個刪除過不了，於是第二次 commit 會卡死。
  那支腳本走完全不需要刪檔的路徑。
- **`.attic/`**：「刪掉一個檔案」實際上只能做到「把它改名搬走」。
  搬到 `.attic/`（已在 `.gitignore` 裡）等於從版本控制的角度消失，
  而老師在自己的電腦上 `rm -rf .attic` 就真的清乾淨。
- **git tag 當保險**：每次大規模移除之前先打一個 tag（`grading-v1`、
  `hosted-v1`），這樣「刪掉」是一個可以反悔的動作。

⚠️ 這一段對接手的人是**必讀**的：不知道這些的話，第二次提交就會卡住，
而錯誤訊息不會告訴你原因。細節在 `CLAUDE.md`。

### 7. 回報的誠實是一條硬規則（第九條）

v0.22 老師明確指定，而它是九條規則裡**唯一沒有任何測試守得住**的一條。
它禁止的事很具體：

- 不得為了讓回報有趣而**故意製造一個 bug 再修好**。
- 不得為了展示除錯能力而繞遠路（「先寫一個笨版本」是被禁止的）。
- 某一輪沒有踩到坑就**據實寫「沒有」**。
- 數字不得注水（測試項數、commit 數、工時）。
- 不確定的事要說成不確定。

理由寫在 `CLAUDE.md` 裡，摘要是：**老師不看每一行程式碼，他依賴回報做判斷**
——而這個系統最大的風險一直都是安靜的錯誤。發現它們的唯一辦法，
是有人如實把「哪裡差點沒被發現」說出來。

> 這條規則的執行機制只有一個：**把理由講到讓人不想繞過**。
> 所以它在 `CLAUDE.md` 裡寫得比其他八條都長。

---

## 三、專案史（重建）

> ⚠️ **這一節是重建的，不是原始紀錄。** 來源是 `PLAN.md` 的變更歷程與
> 決定表、102 個 commit 訊息、`TURNAROUND.csv`。它記得住「做了什麼、
> 決定了什麼」，記不住「老師當初怎麼要求的」。

### 時間軸

| 版本 | 日期 | 這一輪做了什麼 | 推翻了什麼 |
|---|---|---|---|
| v0.1–v0.3 | 08-07 ~ 08-18 | 規劃書；**階段 1 MVP**（學號密碼登入 + 四個題型的出題引擎）；自架 KaTeX／HTMX；介面全面改英文 | — |
| v0.4–v0.6 | 08-18 | **作答判定**（`app/grader/`）：子行程沙箱、逾時、三值 `is_zero`、指數形式統一 | D6/D8：判定子行程叫不起來時 fail fast，不靜默降級 |
| **v0.7** | 08-18 | ⛔ **捨棄自動評分**（D12），保存在 tag `grading-v1`；答案改為預設遮蔽（D13） | **推翻 v0.4–v0.6 整整三輪的產出** |
| v0.8–v0.10 | 08-18 ~ 08-19 | 範圍擴張：加入 Fourier 分析與相圖（D15）；目錄按章節切（D16）；對評分保持沉默（D17） | — |
| v0.11–v0.14 | 08-19 | **第二個功能區：瀏覽器端訊號處理展示**（D18–D22）；學期週次日期落地（D23）；首頁不陳列進度（D24）；三個展示 + FFT 層 + 六個範例音檔 | D28 推翻 D20（改為使用者選本機檔案，完全在瀏覽器處理） |
| v0.15 | 08-20 | 關閉學生自行註冊，改為老師配發帳號（D32–D34） | 推翻 D2 的前半 |
| **v0.16** | 08-26 | ⛔ **改為兩組共用帳號，系統不再蒐集任何個人資料**（D35–D40） | **推翻 D2 後半與 D32/D33/D34**，整個個資法遵面消失 |
| v0.17–v0.20 | 08-26 ~ 08-28 | 2S5 加法合成展示；⛔ **砍掉階段 3（對話介面 + LLM）**（D41）；只開放校內 IP（D42）；維持 HTTPS（D43）；家用預演改 KVM（D44）；官方不支援 Safari（D45） | D41 砍掉一整個規劃階段 |
| v0.21–v0.23 | 08-29 | 2S10 摺積、2S11 脈衝、2S9 極零點三個展示；**牆鐘時間紀錄**（D46）；**第九條硬規則：回報必須是真的** | — |
| v0.24–v0.27 | 08-30 ~ 08-31 | 2f Laplace（D47）；2B0–2B4 Fourier 級數與四層閘門（D48、D49）；2d 系統的重根／複數／非齊次 + 相圖（D50、D51）；2a 待定係數、2b 恰當方程；`VERIFY-CHECKLIST.md` | — |
| v0.28 | 09-01 | 週次歸類與**內容開放閘門**（D52–D56） | — |
| **v0.29** | 09-02 | ⛔ **改為學生自行本機安裝**（D57–D61）：拆掉帳號、資料庫、用量紀錄、開放閘門與全部部署文件 | **推翻 v0.15、v0.16、v0.19、v0.20、v0.28 的主要產出** |

### 從這條時間軸看得出來的三件事

**1. 大約每五輪就有一輪是在拆東西。**
v0.7 拆判定、v0.16 拆帳號模型、v0.18 拆對話介面、v0.29 拆整個站台。
四次都不是因為做壞了——**是因為需求的前提變了**。
⚠️ 這對估工作量有一個直接的意義：一個「已完成」的功能不等於一筆
已經回收的投資。

**2. 被拆掉的東西留下了可以重用的部分，而那不是巧合。**
判分被砍掉時，`Check` 這個物件留了下來，變成出題端的驗證閘門——
而它現在是整個系統唯一的品質保證。v0.29 拆掉開放閘門時，週次歸類留了下來
變成選單分組。**兩次都是因為那個東西當初就被設計成「只做一件事」。**

**3. 拆得掉，是因為每次都先打了 tag。**
`grading-v1` 與 `hosted-v1` 讓「刪掉」變成一個可以反悔的動作。
沒有這一步的話，每一次拆除都會變成一個需要辯論的決定。

### 幾個具代表性的技術判斷

這些是從 `PLAN.md` 的決定表挑出來的，挑選標準是「對別的專案也有意義」：

- **反向構造優先於正向求解**（§2.1）。出題不是「隨機生一個方程再去解」，
  是「先決定答案長什麼樣，再倒推題目」。前者幾乎必然生出解不出來或很醜的
  題目；後者讓漂亮的答案變成免費的。
- **SymPy 是驗證閘門，不是生成器**（規則 1）。每一題出廠前把標準答案代回
  原方程，殘差不是恰好 0 就整組重抽。**「證不出來就當作沒過」**——
  閘門的預設必須是擋下來。
- **`check is None` 一律視為不通過。** 這一條守的是失敗的方向：
  若被當成通過，症狀會是「新題型的每一題都完美無瑕」。
- **四層閘門，而且四層都要跑**（D48，Fourier）。沒有任何一層是充分的，
  所以「這麼多層太慢了」不是一個可以自己下的結論。
- **音訊安全做三層，刻意重疊**（2S9）。每一層單獨看都「應該夠了」，
  而那正是三層必須同時存在的理由——前兩層都在主執行緒上，
  而主執行緒可能卡住；音訊執行緒仍然在跑。
- **`|safe` 的白名單放在建構時，不放在渲染時。** 門的守衛放在
  「東西被造出來」那一刻，比放在「東西被印出來」那一刻早一步。

---

## 四、給接手的人

### 先讀什麼

1. **`CLAUDE.md`** —— 九條硬規則，以及「已經不存在的東西」的清單。
   ⚠️ 那份清單很長，而它防的是一件真的會發生的事：照著一個已經作廢的設計
   往下寫。
2. **`README.md`** —— 現況、專案結構、怎麼新增一個題型。
3. **`PLAN.md`** 的**決定事項表**（D1–D61）—— 不必從頭讀六千行，
   但動任何東西之前先在那張表裡搜一下相關的代號。
4. **`INSTALL-LINUX.md` / `INSTALL-MACOS.md`** —— 使用者看到的是什麼。

### 如果你是**第一個**接手的工作階段

先做一次交接驗收，不要直接開始實作——可以直接複製的第一個提示詞在
**`dispatches/TEMPLATE-first-session.md`**。它做三件事：確認環境
（特別是**能不能刪檔**，那決定了三個慣例要不要遵守）、把 1386 項測試
跑一次、確認兩個 tag 在。

⚠️ **一上來就實作的風險很具體**：測試若因為環境問題而紅，
很容易被誤判成「我剛才改壞了」，然後往錯的方向修。

### 動手之前（每一輪）

```bash
# 1. 把提示詞原文存進 dispatches/（見該目錄的 README）
# 2. 才開始計時
python scripts/turnaround.py start <任務編號> --estimate <人週>
```

### 幾個容易踩的地方

- **不能刪檔**（如果你也在同一種沙箱裡跑）。提交一律用
  `scripts/git-safe-commit.sh`，而且**路徑參數要放在 `--` 後面**，
  不然它會 `git add -A` 把整輪塞進一個 commit。
- **不要把 git 的輸出接到 `head`。** SIGPIPE 會讓腳本在產生 commit 之前
  就死掉，而它看起來像成功了。（這兩件事 v0.28 那一輪各踩了一次，
  記在 `dispatches/2026-09-01-v0.28-week-gating.md` 裡。）
- **測試要分批跑。** 全套 1305 項約 10–12 分鐘，而沙箱單次指令約 180 秒
  上限。`tests/test_generators.py` 要分九批，切法寫在 `CLAUDE.md`。
- **`app/curriculum.py` 要跟著更新。** 新增題型或展示忘了在那裡補一列，
  它就不會出現在選單上，而且不會報錯。

### 這個專案目前最誠實的一句話

**它的六個互動展示，從來沒有在真正的瀏覽器裡被驗收過。** 開發環境是一個
沒有瀏覽器的 Linux 沙箱；數值層有 405 項由 node 驅動的測試，版面與聲音
只有 `scripts/run_demo_smoke.mjs` 在假 DOM 裡跑過。相圖的箭頭也是同一類
缺口（用 `cairosvg` 光柵化再自己看圖，而它不是 Chrome 也不是 Firefox）。

**這一項不會因為改成本機安裝而消失，反而變得更容易解決**——現在任何一個
學生打開它，就是一次真實的跨瀏覽器測試。逐項的檢查清單在
`VERIFY-CHECKLIST.md`。
