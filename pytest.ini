[pytest]
testpaths = tests
markers =
    dsp_js: 由 node 驅動的瀏覽器端 DSP 數值測試（PLAN.md §8.4 方案 B）。
        快速回歸想跳過時用 `pytest -m "not dsp_js"`；缺 node 時會 skip 並印出原因。
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
