# 完成 2c，以及把兩個「靠人記得」的縫改成機制

**日期**：2026-09-04　**工作項**：**2c** ＋ **GUARDS**（新）
**狀態**：進行中（本檔案在開工的第一件事就建立，見 `dispatches/README.md`）

---

## 提示詞（逐字）

請繼續完成 2c。此外，您提到還不夠穩的第二第三點，是否若實作皆是由您進行而人類沒有手動改 code ，問題就不會發生？您能否建立一些規則來確保若是您進行，這兩類問題就不會發生？

### 「第二第三點」指的是什麼

上一輪（AUTOMAP，相依地圖自動更新）回報末尾「還不夠穩的地方」的第 2、3 點：

2. **「跑測試」與「更新地圖」現在綁在一起，而人可以繞過去。**
   直接打 `pytest` 仍然會跑測試，但不會更新地圖。
3. **一條沒有任何測試碰得到的新相依仍然看不見。**
   例如有人寫了一個新的 generator 但忘了在 `app/generator/__init__.py` 加匯入
   ——沒有任何測試會碰到它。

---

## 回覆（摘要；完整版見本輪最終訊息）

### 2c：先問了範圍，老師選「只修附錄 C 的符號不一致」

2c 有一半可以執行（附錄 C 已經拍板的規範），一半不行（英文用語要對課本、
步驟顆粒度要老師定規則、補寫缺的 `note` 是教學文字）。用 `AskUserQuestion` 問過。

修的是 `separable.py` 與 `first_order_linear.py` 的步驟 LaTeX：
未知函數全程寫 $y(x)$。⛔ **一句 `note` 或 `title` 都沒有動。**
新增 `test_the_unknown_function_keeps_its_argument_in_every_step` 防止復發，
另加一項突變測試證明那個正規式真的擋得住舊寫法。

⚠️ **找到一個自己的誤報**：`ode.laplace.ivp` 被 v0.32 的初篩標成違反，
但附錄 C.3 的「導數的變換」那一列**逐字**就是
`L\{y'\} = s\,L\{y\} - y(0)`。**上一輪回報寫「三處都是滿的 15/15」，
那句話是錯的，實際是兩處。**

⛔ **修完之後暴露出一件本來被遮住的事**：`separable` 難度 3 的
「Solve for y」與「General solution」現在是兩行一模一樣的字。
那個重複一直都在，只是被符號不一致遮住了。**沒有動它**——刪掉一個步驟
是步驟結構的判斷（§7 #22），那是老師的。

### 老師的問題：那兩類問題會不會因為「只有 AI 實作」就不發生？

**兩個問題的答案不一樣，而那個差別才是重點。**

| | 會不會因為「只有 AI 實作」就消失 | 做了什麼 |
|---|---|---|
| 第 2 點（直接跑 `pytest` 不更新地圖） | **實務上會，但它是一條靠人記得的規則** | 把鉤子搬進 `tests/conftest.py`（D69）——**pytest 一定會載入 conftest**，所以那個繞道不存在了 |
| 第 3 點（沒有人 import 的新檔案） | ⛔ **不會。與是誰在打字無關** | 加一項測試（D70）：`app/` 底下不准有沒被 import 的 `.py` |

⛔ **第 3 點值得說清楚**：AI 一樣會寫好一個新的 generator、`@register(...)`
也寫好，**卻忘了在 `app/generator/__init__.py` 加那一行 import**。
症狀是 1197 項全綠而那個檔案是死的。**那不是紀律問題，是缺一項測試。**

### 踩到的坑

**一個，而且它一度癱瘓了機器。** conftest 的自動記錄在 `--collect-only`
的子行程裡又觸發了一次自動記錄——**無限遞迴地生出 pytest 子行程**，
背景一度有 61 個，終端機沒有任何輸出、只是不會結束。
兩道防護：`_collected()` 的子行程強制 `TEST_DEPS_AUTOUPDATE=0`，
以及 conftest 對 `--collect-only` 直接跳過。

⚠️ 順帶：`.pytest_cache/` 原本被記成相依，而它每一次跑都會變
——那會讓**每個測試檔永遠是「過期」的**，把新鮮度這個訊號整個弄成雜訊。已排除。
