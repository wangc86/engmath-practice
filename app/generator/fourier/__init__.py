"""Fourier 分析的題型（PLAN.md §2.10、階段 2B 的 2B1–2B4；課綱 W3）。

三個題型，共用 `core.py` 的一整套機器：

- ``fourier.series.full_range``  — 全幅 Fourier 級數（2B2）
- ``fourier.series.half_range``  — 半幅展開（正弦／餘弦級數，2B3）
- ``fourier.symmetry.parity``    — 奇偶性與係數消失（2B4）

⚠️ **這個子目錄與 `ode/` 一樣是 D16 的目標結構**，而既有四個 ODE 題型仍在
上一層的平面結構裡。理由見 `ode/__init__.py`：搬檔案需要 unlink，
自動化工作階段做不到，工作項 2a0 保留給老師在自己的電腦上執行。
`template_id` 與檔案路徑從來沒有耦合過，所以混著放沒有行為上的後果。

---

## 這一包與 ODE 那四個最大的不同

**沒有方程可以代回去。** ODE 的驗證閘門有一條天然的獨立路徑（把答案代回原式），
Fourier 的答案是「一組由積分定義的係數」，而那條積分**就是生成它的路徑**——
拿定義式重算一次只是把同一段程式再跑一遍。

所以這裡的閘門是自己造出來的四層，寫在 `core.FourierCheck` 的 docstring 裡。
**四層不是保守，是因為沒有一層是充分的。**
"""
